<!DOCTYPE html>
<html>
    <body>
        <h1>{{ title }}</h1>
        <p>{{ content }}</p>
        <hr>
        <ul>
            {% for post in posts %}
                <li><a href="{{ post.url }}">{{ post.title }}</a></li>
            {% endfor %}
        </ul>
    </body>
</html>